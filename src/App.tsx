/**
 * Home Be Wild — agora 100% espelhada do reference HTML/CSS aprovado.
 * Toda a Home (nav, hero, stack, lâminas, tabs, jornada, bairros,
 * simulador, CTA final, footer) vive em src/pages/HomeBw.tsx + CSS
 * dedicado em src/styles/bw-home.css.
 */
import HomeBw from "./pages/HomeBw";

export default function App() {
  return <HomeBw />;
}
